apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: model
spec:
  strategy:
    blueGreen:
      postPromotionAnalysis:
        templates:
        - templateName: slo-post
          args:
          - name: namespace
            value: model-prod
          - name: service
            value: model-active
